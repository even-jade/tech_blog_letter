# 2026-08-03 기술 블로그 모음

## [벡터DB를 걷어내고 유사글 추천 되살리기 — 임베딩 배치 Agent 개발기](http://devocean.sk.com/blog/techBoardDetail.do?id=168411&boardType=techBlog&isShared=Y)
- 회사: SK텔레콤 (DEVOCEAN)
- 본문 요약: 비용 부담으로 없앤 벡터DB 대신 MySQL에 임베딩을 저장하고 배치로 전수 유사도를 계산해 유사글 추천 기능을 상시 인프라 없이 되살렸습니다. Gemini 임베딩의 taskType(SEMANTIC_SIMILARITY)을 활용하고, 본문 대신 요약 텍스트로 임베딩하며 증분 색인·배치별 즉시 저장으로 안정성을 높였습니다.
- 주목할 point: 코사인 유사도 하한값(0.85)을 감이 아니라 실측 데이터로 정한 과정, "실패=API 전면 장애"라는 방어 로직의 조건이 실제 의미와 어긋나 캐시 900건이 멀쩡한데도 추천 갱신을 스킵했던 사고 사례, 빈 API 키·git 의미 충돌(semantic conflict) 트러블슈팅 경험담이 인상적입니다.
- keyword: #MySQL #벡터DB #Gemini #임베딩

## [통합테스트 시나리오 설계와 관리](https://www.nextree.io/tonghabteseuteu-sinario-seolgyewa-gwanri/)
- 회사: 넥스트리
- 본문 요약: 설문 관리 서비스의 통합 테스트를 요구사항 정의서 기반에서 사용자 흐름 중심의 TS(시나리오)/TC(케이스) 2단계 구조로 재설계했습니다. 대상(역할)·구분(기능영역) 두 축으로 케이스를 분류하고, dev/stg/prod 환경별로 검증 목적을 다르게 설정했습니다.
- 주목할 point: 필수값/형식/상태/정책성 네 가지 검증 패턴을 반복 적용해 Happy Path 밖 예외 케이스를 체계적으로 도출한 방식, PASS/FAIL/ABORTED/BLOCKED 4단계 결과 분류와 "한 번 부여된 ID는 덮어쓰지 않는다"는 원칙으로 장기 추적성을 확보한 점이 실무적으로 참고할 만합니다.
- keyword: #test #QA #회귀테스트

## [애플리케이션 계층에서 관계 다루기](https://www.nextree.io/aepeulrikeisyeon-gyeceungeseo-gwangye-darugi/)
- 회사: 넥스트리
- 본문 요약: DB 외래키·JPA cascade 대신 애플리케이션 코드에서 엔티티 간 관계를 다루는 이유와 구조를 정리했습니다. Flow-Logic-PolicyHandler-CascadeContext로 역할을 나눠 DataEvent 기반으로 관계를 전파하고, 여러 경로로 같은 엔티티에 중복 도달해도 한 번만 처리되도록 설계했습니다.
- 주목할 point: FieldSourceId(참조 그래프)와 RelationPolicy(전파 그래프) 두 메타데이터를 비교해 누락된 정책·중복 경로·순환 경로를 관찰 가능하게 만든 설계, CascadeContext에 dry-run 플래그를 더해 영향 범위를 미리 계산하는 확장 아이디어가 눈에 띕니다.
- keyword: #JPA #DB #아키텍처

## ["같은 트래픽, CPU는 86% 덜 쓴다" — 100만 사이트 플랫폼 아임웹의 Valkey 9.1 실측 결과](https://aws.amazon.com/ko/blogs/tech/aws-imweb-elasticache-tech-blog/)
- 회사: 아임웹 (AWS 공동 작성)
- 본문 요약: 아임웹이 Amazon ElastiCache 기반 캐시를 Redis 6.x/Valkey 7.2에서 Valkey 9.1로 업그레이드한 실측 사례를 공유했습니다. I/O 스레딩 재설계, 새 해시테이블, zero-copy 응답 등 엔진 개선으로 전체 CPU 86.6%, GET/SET 지연 30~54% 감소를 확인했습니다.
- 주목할 point: 엔진 업그레이드와 노드 다운사이징을 동시에 진행하지 않아 성능 변화의 원인을 CPU/메모리/버전 중 무엇 때문인지 명확히 분리한 접근, 클라이언트 쪽 connection/command retry·offline queue 정책을 사전 점검해 업그레이드 중 영향을 최소화한 과정, 연간 약 7,560만 원의 비용 절감 추정치가 구체적입니다.
- keyword: #Valkey #Redis #ElastiCache

## [DS와 MLE가 함께 일하는 법](https://toss.tech/article/ds-mle-cowork)
- 회사: 토스뱅크
- 본문 요약: DS(Data Scientist)와 MLE(ML Engineer) 간 모델 인계 방식을 1년간 세 단계로 발전시킨 과정을 정리했습니다. 노트북 통째 전달 → 추론 로직만 .py로 분리 → 추상화 클래스 기반 인터페이스(전처리·추론·후처리) 계약으로 진화하며 서빙 전환 비용을 크게 줄였습니다.
- 주목할 point: 모델 서빙 코드를 pip install 가능한 패키지로 만들어 uv 워크스페이스 모노레포로 관리한 점, 최근에는 AI가 작성한 코드의 스타일까지 팀 컨벤션(pfmls-stylepack)으로 통일해야 하는 새로운 협업 과제가 생겼다는 지적이 시사점을 줍니다.
- keyword: #LLM #ML #모노레포

## [분산 학습을 위한 AWS 컴퓨트 선택 가이드 (2편: 초대규모 스케일링과 인스턴스 확보)](https://aws.amazon.com/ko/blogs/tech/aws-compute-distributed-training-guide-massive-scaling/)
- 회사: AWS
- 본문 요약: 수천~수만 GPU를 하나로 묶는 울트라클러스터(EFA 기반 non-blocking 패브릭)와 울트라서버(NVLink로 최대 72 GPU를 하나의 메모리 풀처럼 묶는 구조) 아키텍처를 설명하고, 고성능 GPU 확보 수단인 ODCR과 Capacity Block의 차이를 비교했습니다.
- 주목할 point: 통신이 잦은 Tensor Parallel/MoE는 NVLink 도메인(72GPU) 안에 가두고, 빈도 낮은 Pipeline/Data Parallel은 EFA 경계를 넘기라는 병렬화 배치 원칙, ODCR(주차장 정기권)·Capacity Block(호텔 예약) 비유로 정리한 예약 전략 선택 기준이 실용적입니다.
- keyword: #AWS #EC2 #분산학습

## [AI와 개발하기: 숨은 결정을 드러내기](https://meetup.nhncloud.com/posts/419)
- 회사: NHN Cloud
- 본문 요약: AI가 코드 대부분을 작성하는 시대에 개발자의 통제 지점이 "코드 작성"에서 "의도 전달과 검증"으로 옮겨갔다는 문제의식에서, 요구사항 이면에 숨은 기술 결정(Atomicity, Idempotency, Failure-mode 등)을 스펙 단계에서 먼저 드러내는 방법론을 제시했습니다.
- 주목할 point: 오픈소스 인터뷰 패턴 "grill-me"(한 번에 한 질문, 추천안 함께 제시, 코드 근거로 질문, 결정이 갈리는 순서로 진행)를 스펙 설계용으로 응용한 점, 이 방식이 구현 전 인지 부하뿐 아니라 리뷰 시점의 검증 기준까지 바꿔준다는 관찰이 흥미롭습니다.
- keyword: #AI #스펙설계 #개발생산성
